"use client";
import { n as __exportAll, r as __toESM } from "./chunk-BoAXSpZd.js";
import { t as require_react } from "./react.js";
import { t as require_jsx_runtime } from "./react_jsx-runtime.js";
import { t as e } from "./dist-Cw0cbGZm.js";
//#region node_modules/fumadocs-core/dist/toc.js
var toc_exports = /* @__PURE__ */ __exportAll({
	AnchorProvider: () => AnchorProvider,
	ScrollProvider: () => ScrollProvider,
	TOCItem: () => TOCItem,
	useActiveAnchor: () => useActiveAnchor,
	useActiveAnchors: () => useActiveAnchors,
	useItems: () => useItems
});
var import_react = /* @__PURE__ */ __toESM(require_react(), 1);
var import_jsx_runtime = /* @__PURE__ */ __toESM(require_jsx_runtime(), 1);
function mergeRefs(...refs) {
	return (value) => {
		refs.forEach((ref) => {
			if (typeof ref === "function") ref(value);
			else if (ref != null) ref.current = value;
		});
	};
}
var ItemsContext = (0, import_react.createContext)(null);
var ScrollContext = (0, import_react.createContext)(null);
/** Optional: add auto-scroll to TOC items. */
function ScrollProvider({ containerRef, children }) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ScrollContext, {
		value: containerRef,
		children
	});
}
function AnchorProvider({ toc, single = false, children }) {
	const observer = (0, import_react.useMemo)(() => new Observer(), []);
	const [items, setItems] = (0, import_react.useState)(observer.items);
	observer.single = single;
	(0, import_react.useEffect)(() => {
		observer.setItems(toc);
	}, [observer, toc]);
	(0, import_react.useEffect)(() => {
		observer.watch({
			rootMargin: "0px",
			threshold: .98
		});
		observer.onChange = () => setItems(observer.items);
		return () => {
			observer.unwatch();
		};
	}, [observer]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ItemsContext, {
		value: items,
		children
	});
}
function TOCItem({ ref, onActiveChange = () => null, ...props }) {
	const id = props.href ? getItemId(props.href) : null;
	const items = useItems();
	const itemData = id ? items.find((item) => item.id === id) : null;
	const containerRef = (0, import_react.use)(ScrollContext);
	const isInitialMountRef = (0, import_react.useRef)(true);
	const anchorRef = (0, import_react.useRef)(null);
	const [active, setActive] = (0, import_react.useState)(() => itemData != null && itemData.active);
	if (itemData && itemData.active !== active) {
		setActive(itemData.active);
		onActiveChange(itemData.active);
	}
	const shouldScroll = itemData?.active && items.every((item) => !item.active || item.t <= itemData.t);
	(0, import_react.useEffect)(() => {
		const anchor = anchorRef.current;
		const container = containerRef?.current;
		if (shouldScroll && container && anchor) e(anchor, {
			behavior: isInitialMountRef.current ? "instant" : "smooth",
			block: "center",
			inline: "center",
			scrollMode: "always",
			boundary: container
		});
		isInitialMountRef.current = false;
	}, [shouldScroll, containerRef]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("a", {
		ref: mergeRefs(anchorRef, ref),
		"data-active": active,
		...props
	});
}
/**
* The estimated active heading ID
*/
function useActiveAnchor() {
	const items = useItems();
	return (0, import_react.useMemo)(() => {
		let out;
		for (const item of items) {
			if (!item.active) continue;
			if (!out || item.t > out.t) out = item;
		}
		return out?.id;
	}, [items]);
}
/**
* The id of visible anchors
*/
function useActiveAnchors() {
	const items = useItems();
	return (0, import_react.useMemo)(() => {
		const out = [];
		for (const item of items) if (item.active) out.push(item.id);
		return out;
	}, [items]);
}
function useItems() {
	const ctx = (0, import_react.use)(ItemsContext);
	if (!ctx) throw new Error(`Component must be used under the <AnchorProvider /> component.`);
	return ctx;
}
function getItemId(url) {
	if (url.startsWith("#")) return url.slice(1);
	return null;
}
var Observer = class {
	constructor() {
		this.items = [];
		this.single = false;
		this.observer = null;
	}
	callback(entries) {
		if (entries.length === 0) return;
		let hasActive = false;
		this.items = this.items.map((item) => {
			const entry = entries.find((entry) => entry.target.id === item.id);
			let active = entry ? entry.isIntersecting : item.active && !item.fallback;
			if (this.single && hasActive) active = false;
			if (item.active !== active) item = {
				...item,
				t: Date.now(),
				active,
				fallback: false
			};
			if (active) hasActive = true;
			return item;
		});
		if (!hasActive && entries[0].rootBounds) {
			const viewTop = entries[0].rootBounds.top;
			let min = Number.MAX_VALUE;
			let fallbackIdx = -1;
			for (let i = 0; i < this.items.length; i++) {
				const element = document.getElementById(this.items[i].id);
				if (!element) continue;
				const d = Math.abs(viewTop - element.getBoundingClientRect().top);
				if (d < min) {
					fallbackIdx = i;
					min = d;
				}
			}
			if (fallbackIdx !== -1) this.items[fallbackIdx] = {
				...this.items[fallbackIdx],
				active: true,
				fallback: true,
				t: Date.now()
			};
		}
		this.onChange?.();
	}
	setItems(newItems) {
		const observer = this.observer;
		if (observer) for (const item of this.items) {
			const element = document.getElementById(item.id);
			if (!element) continue;
			observer.unobserve(element);
		}
		this.items = [];
		for (const item of newItems) {
			const id = getItemId(item.url);
			if (!id) continue;
			this.items.push({
				id,
				active: false,
				fallback: false,
				t: 0,
				original: item
			});
		}
		this.watchItems();
	}
	watch(options) {
		if (this.observer) return;
		this.observer = new IntersectionObserver(this.callback.bind(this), options);
		this.watchItems();
	}
	watchItems() {
		if (!this.observer) return;
		for (const item of this.items) {
			const element = document.getElementById(item.id);
			if (!element) continue;
			this.observer.observe(element);
		}
	}
	unwatch() {
		this.observer?.disconnect();
		this.observer = null;
	}
};
//#endregion
export { AnchorProvider, ScrollProvider, TOCItem, toc_exports as t, useActiveAnchor, useActiveAnchors, useItems };

//# sourceMappingURL=fumadocs-core_toc.js.map