import{r as e}from"./chunk-0oqf_uz2.js";import{t}from"./jsx-runtime-CEHt124i.js";var n=e(t()),r={title:`Configuration`,description:`Customize the Voltus SDK behavior with comprehensive configuration options.`,icon:`Settings`},i=`

The Voltus SDK is designed to work out of the box with sensible defaults, but nearly every aspect of its behavior can be customized. From timeouts and retry policies to custom logging and request middleware, the configuration system gives you full control without adding complexity when you don't need it.

Introduction [#introduction]

Most applications will only ever set two or three configuration options — the API key and maybe the environment. But the full range of options is there for when you need precise control over the SDK's behavior in production.

Design Philosophy [#design-philosophy]

We follow the principle of "sensible defaults, full overrides." Every configuration option has a default that works well for the majority of use cases. You only need to specify options when you want behavior that differs from the default. This keeps simple integrations simple while giving power users the flexibility they need.

When to Customize [#when-to-customize]

If the SDK works well for you with just an API key, don't change anything else. Start customizing when you encounter specific needs — like setting longer timeouts for slow networks, reducing retries for time-sensitive operations, or adding middleware for observability.

Client Options [#client-options]

The \`VoltusClient\` constructor accepts a configuration object that controls every aspect of the SDK's behavior. Here's what you can configure and why you might want to.

Connection Settings [#connection-settings]

The \`timeout\` option controls how long the SDK waits for a response before giving up. The default is 30 seconds, which works for most operations. If you're exporting large datasets, you might increase this. If you need fast failure for user-facing requests, you might decrease it. The \`connectTimeout\` option separately controls how long the SDK waits to establish a connection.

Retry Policy [#retry-policy]

By default, failed requests are retried up to three times with exponential backoff — one second, then two seconds, then four seconds. You can adjust the \`retries\` count, the \`retryDelay\` base interval, and the \`retryBackoff\` strategy. Setting \`retries\` to zero disables automatic retries entirely, which can be useful for idempotency-sensitive operations.

Middleware [#middleware]

The SDK's middleware system lets you hook into the request lifecycle for logging, monitoring, and custom header injection.

Request Interceptors [#request-interceptors]

Register middleware using \`client.use()\` with an object containing \`onRequest\`, \`onResponse\`, and \`onError\` handlers. The \`onRequest\` handler receives the full request configuration and can modify headers, log the outgoing request, or inject tracing identifiers before the request is sent.

Response Interceptors [#response-interceptors]

The \`onResponse\` handler receives the parsed response including status code, headers, and duration. This is the ideal place to log response times, record metrics, or trigger alerts when latency exceeds a threshold. Middleware runs in the order it's registered, so you can layer multiple concerns without them interfering with each other.

Environment Configuration [#environment-configuration]

Production applications need different settings for different deployment stages. The SDK makes it easy to derive configuration from environment variables.

Development vs Production [#development-vs-production]

Your development environment might use the sandbox API with verbose debug logging, while production uses the live API with minimal logging and tighter timeouts. The SDK's \`environment\` option switches between API endpoints, and you can combine this with a \`logLevel\` setting to control verbosity.

Configuration Factory [#configuration-factory]

We recommend creating a factory function that reads environment variables and returns a validated configuration object. This function becomes the single place where all environment-specific logic lives, and it can validate that required variables are present before your application starts accepting traffic.
`,a={contents:[{heading:void 0,content:`The Voltus SDK is designed to work out of the box with sensible defaults, but nearly every aspect of its behavior can be customized. From timeouts and retry policies to custom logging and request middleware, the configuration system gives you full control without adding complexity when you don't need it.`},{heading:`introduction`,content:`Most applications will only ever set two or three configuration options — the API key and maybe the environment. But the full range of options is there for when you need precise control over the SDK's behavior in production.`},{heading:`design-philosophy`,content:`We follow the principle of "sensible defaults, full overrides." Every configuration option has a default that works well for the majority of use cases. You only need to specify options when you want behavior that differs from the default. This keeps simple integrations simple while giving power users the flexibility they need.`},{heading:`when-to-customize`,content:`If the SDK works well for you with just an API key, don't change anything else. Start customizing when you encounter specific needs — like setting longer timeouts for slow networks, reducing retries for time-sensitive operations, or adding middleware for observability.`},{heading:`client-options`,content:"The `VoltusClient` constructor accepts a configuration object that controls every aspect of the SDK's behavior. Here's what you can configure and why you might want to."},{heading:`connection-settings`,content:"The `timeout` option controls how long the SDK waits for a response before giving up. The default is 30 seconds, which works for most operations. If you're exporting large datasets, you might increase this. If you need fast failure for user-facing requests, you might decrease it. The `connectTimeout` option separately controls how long the SDK waits to establish a connection."},{heading:`retry-policy`,content:"By default, failed requests are retried up to three times with exponential backoff — one second, then two seconds, then four seconds. You can adjust the `retries` count, the `retryDelay` base interval, and the `retryBackoff` strategy. Setting `retries` to zero disables automatic retries entirely, which can be useful for idempotency-sensitive operations."},{heading:`middleware`,content:`The SDK's middleware system lets you hook into the request lifecycle for logging, monitoring, and custom header injection.`},{heading:`request-interceptors`,content:"Register middleware using `client.use()` with an object containing `onRequest`, `onResponse`, and `onError` handlers. The `onRequest` handler receives the full request configuration and can modify headers, log the outgoing request, or inject tracing identifiers before the request is sent."},{heading:`response-interceptors`,content:"The `onResponse` handler receives the parsed response including status code, headers, and duration. This is the ideal place to log response times, record metrics, or trigger alerts when latency exceeds a threshold. Middleware runs in the order it's registered, so you can layer multiple concerns without them interfering with each other."},{heading:`environment-configuration`,content:`Production applications need different settings for different deployment stages. The SDK makes it easy to derive configuration from environment variables.`},{heading:`development-vs-production`,content:"Your development environment might use the sandbox API with verbose debug logging, while production uses the live API with minimal logging and tighter timeouts. The SDK's `environment` option switches between API endpoints, and you can combine this with a `logLevel` setting to control verbosity."},{heading:`configuration-factory`,content:`We recommend creating a factory function that reads environment variables and returns a validated configuration object. This function becomes the single place where all environment-specific logic lives, and it can validate that required variables are present before your application starts accepting traffic.`}],headings:[{id:`introduction`,content:`Introduction`},{id:`design-philosophy`,content:`Design Philosophy`},{id:`when-to-customize`,content:`When to Customize`},{id:`client-options`,content:`Client Options`},{id:`connection-settings`,content:`Connection Settings`},{id:`retry-policy`,content:`Retry Policy`},{id:`middleware`,content:`Middleware`},{id:`request-interceptors`,content:`Request Interceptors`},{id:`response-interceptors`,content:`Response Interceptors`},{id:`environment-configuration`,content:`Environment Configuration`},{id:`development-vs-production`,content:`Development vs Production`},{id:`configuration-factory`,content:`Configuration Factory`}]},o=[{depth:2,url:`#introduction`,title:(0,n.jsx)(n.Fragment,{children:`Introduction`})},{depth:3,url:`#design-philosophy`,title:(0,n.jsx)(n.Fragment,{children:`Design Philosophy`})},{depth:3,url:`#when-to-customize`,title:(0,n.jsx)(n.Fragment,{children:`When to Customize`})},{depth:2,url:`#client-options`,title:(0,n.jsx)(n.Fragment,{children:`Client Options`})},{depth:3,url:`#connection-settings`,title:(0,n.jsx)(n.Fragment,{children:`Connection Settings`})},{depth:3,url:`#retry-policy`,title:(0,n.jsx)(n.Fragment,{children:`Retry Policy`})},{depth:2,url:`#middleware`,title:(0,n.jsx)(n.Fragment,{children:`Middleware`})},{depth:3,url:`#request-interceptors`,title:(0,n.jsx)(n.Fragment,{children:`Request Interceptors`})},{depth:3,url:`#response-interceptors`,title:(0,n.jsx)(n.Fragment,{children:`Response Interceptors`})},{depth:2,url:`#environment-configuration`,title:(0,n.jsx)(n.Fragment,{children:`Environment Configuration`})},{depth:3,url:`#development-vs-production`,title:(0,n.jsx)(n.Fragment,{children:`Development vs Production`})},{depth:3,url:`#configuration-factory`,title:(0,n.jsx)(n.Fragment,{children:`Configuration Factory`})}];function s(e){let t={code:`code`,h2:`h2`,h3:`h3`,p:`p`,...e.components};return(0,n.jsxs)(n.Fragment,{children:[(0,n.jsx)(t.p,{children:`The Voltus SDK is designed to work out of the box with sensible defaults, but nearly every aspect of its behavior can be customized. From timeouts and retry policies to custom logging and request middleware, the configuration system gives you full control without adding complexity when you don't need it.`}),`
`,(0,n.jsx)(t.h2,{id:`introduction`,children:`Introduction`}),`
`,(0,n.jsx)(t.p,{children:`Most applications will only ever set two or three configuration options — the API key and maybe the environment. But the full range of options is there for when you need precise control over the SDK's behavior in production.`}),`
`,(0,n.jsx)(t.h3,{id:`design-philosophy`,children:`Design Philosophy`}),`
`,(0,n.jsx)(t.p,{children:`We follow the principle of "sensible defaults, full overrides." Every configuration option has a default that works well for the majority of use cases. You only need to specify options when you want behavior that differs from the default. This keeps simple integrations simple while giving power users the flexibility they need.`}),`
`,(0,n.jsx)(t.h3,{id:`when-to-customize`,children:`When to Customize`}),`
`,(0,n.jsx)(t.p,{children:`If the SDK works well for you with just an API key, don't change anything else. Start customizing when you encounter specific needs — like setting longer timeouts for slow networks, reducing retries for time-sensitive operations, or adding middleware for observability.`}),`
`,(0,n.jsx)(t.h2,{id:`client-options`,children:`Client Options`}),`
`,(0,n.jsxs)(t.p,{children:[`The `,(0,n.jsx)(t.code,{children:`VoltusClient`}),` constructor accepts a configuration object that controls every aspect of the SDK's behavior. Here's what you can configure and why you might want to.`]}),`
`,(0,n.jsx)(t.h3,{id:`connection-settings`,children:`Connection Settings`}),`
`,(0,n.jsxs)(t.p,{children:[`The `,(0,n.jsx)(t.code,{children:`timeout`}),` option controls how long the SDK waits for a response before giving up. The default is 30 seconds, which works for most operations. If you're exporting large datasets, you might increase this. If you need fast failure for user-facing requests, you might decrease it. The `,(0,n.jsx)(t.code,{children:`connectTimeout`}),` option separately controls how long the SDK waits to establish a connection.`]}),`
`,(0,n.jsx)(t.h3,{id:`retry-policy`,children:`Retry Policy`}),`
`,(0,n.jsxs)(t.p,{children:[`By default, failed requests are retried up to three times with exponential backoff — one second, then two seconds, then four seconds. You can adjust the `,(0,n.jsx)(t.code,{children:`retries`}),` count, the `,(0,n.jsx)(t.code,{children:`retryDelay`}),` base interval, and the `,(0,n.jsx)(t.code,{children:`retryBackoff`}),` strategy. Setting `,(0,n.jsx)(t.code,{children:`retries`}),` to zero disables automatic retries entirely, which can be useful for idempotency-sensitive operations.`]}),`
`,(0,n.jsx)(t.h2,{id:`middleware`,children:`Middleware`}),`
`,(0,n.jsx)(t.p,{children:`The SDK's middleware system lets you hook into the request lifecycle for logging, monitoring, and custom header injection.`}),`
`,(0,n.jsx)(t.h3,{id:`request-interceptors`,children:`Request Interceptors`}),`
`,(0,n.jsxs)(t.p,{children:[`Register middleware using `,(0,n.jsx)(t.code,{children:`client.use()`}),` with an object containing `,(0,n.jsx)(t.code,{children:`onRequest`}),`, `,(0,n.jsx)(t.code,{children:`onResponse`}),`, and `,(0,n.jsx)(t.code,{children:`onError`}),` handlers. The `,(0,n.jsx)(t.code,{children:`onRequest`}),` handler receives the full request configuration and can modify headers, log the outgoing request, or inject tracing identifiers before the request is sent.`]}),`
`,(0,n.jsx)(t.h3,{id:`response-interceptors`,children:`Response Interceptors`}),`
`,(0,n.jsxs)(t.p,{children:[`The `,(0,n.jsx)(t.code,{children:`onResponse`}),` handler receives the parsed response including status code, headers, and duration. This is the ideal place to log response times, record metrics, or trigger alerts when latency exceeds a threshold. Middleware runs in the order it's registered, so you can layer multiple concerns without them interfering with each other.`]}),`
`,(0,n.jsx)(t.h2,{id:`environment-configuration`,children:`Environment Configuration`}),`
`,(0,n.jsx)(t.p,{children:`Production applications need different settings for different deployment stages. The SDK makes it easy to derive configuration from environment variables.`}),`
`,(0,n.jsx)(t.h3,{id:`development-vs-production`,children:`Development vs Production`}),`
`,(0,n.jsxs)(t.p,{children:[`Your development environment might use the sandbox API with verbose debug logging, while production uses the live API with minimal logging and tighter timeouts. The SDK's `,(0,n.jsx)(t.code,{children:`environment`}),` option switches between API endpoints, and you can combine this with a `,(0,n.jsx)(t.code,{children:`logLevel`}),` setting to control verbosity.`]}),`
`,(0,n.jsx)(t.h3,{id:`configuration-factory`,children:`Configuration Factory`}),`
`,(0,n.jsx)(t.p,{children:`We recommend creating a factory function that reads environment variables and returns a validated configuration object. This function becomes the single place where all environment-specific logic lives, and it can validate that required variables are present before your application starts accepting traffic.`})]})}function c(e={}){let{wrapper:t}=e.components||{};return t?(0,n.jsx)(t,{...e,children:(0,n.jsx)(s,{...e})}):s(e)}export{i as _markdown,c as default,r as frontmatter,a as structuredData,o as toc};