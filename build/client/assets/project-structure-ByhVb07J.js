import{r as e}from"./chunk-0oqf_uz2.js";import{t}from"./jsx-runtime-CEHt124i.js";var n=e(t()),r={title:`Project Structure`,description:`Recommended project structure for organizing your Voltus SDK integration.`,icon:`Folder`},i=`

As your integration grows beyond a single file, having a clear and consistent project structure becomes essential. The patterns described here represent what we've seen work well across hundreds of production integrations.

Introduction [#introduction]

There's no single "correct" way to organize your code when using the Voltus SDK. The patterns here are recommendations based on real-world experience, not requirements. That said, following a consistent structure will make your codebase easier to navigate, test, and maintain — especially as your team grows and more people touch the integration code.

When Structure Matters [#when-structure-matters]

For a quick prototype or script, you can put everything in a single file and it'll work perfectly fine. But once your integration touches multiple resources, handles webhooks, manages caching, or runs in production with monitoring, having separate modules for each concern prevents your code from becoming tangled and hard to reason about.

The Recommended Layout [#the-recommended-layout]

We suggest creating a dedicated directory for your Voltus integration — something like \`src/voltus/\` or \`lib/voltus/\`. Inside this directory, you'll have separate files for the client instance, each resource type's service layer, webhook handlers, and custom type definitions. This keeps all integration-related code in one place while maintaining clean separation between concerns.

Client Module [#client-module]

The most important architectural decision is where your client instance lives. A centralized client module ensures every part of your application uses the same configuration and connection pool.

Single Source of Truth [#single-source-of-truth]

Create a file called \`client.ts\` that initializes the \`VoltusClient\` with your configuration and exports it. Every other module that needs to interact with the Voltus API imports the client from this one file. This means changes to your configuration — like updating the timeout or adding middleware — only need to happen in one place.

Avoiding Common Mistakes [#avoiding-common-mistakes]

The most common mistake we see is creating new client instances inline wherever they're needed. While this technically works, you end up with duplicated configuration scattered across your codebase, multiple connection pools competing for resources, and no central place to add cross-cutting concerns like logging or error handling. A single shared client instance solves all of these problems.

Service Layer [#service-layer]

Wrapping your SDK calls in service functions creates a clean separation between your API integration and the rest of your application logic.

Why Use Services [#why-use-services]

Instead of calling \`client.resources.list()\` directly in your route handlers or React components, create functions like \`getAllResources()\` or \`findResourceByName()\` that encapsulate the business logic. Your route handler calls the service function, which handles pagination, caching, error recovery, and data transformation internally. The handler just gets back the data it needs.

Organizing Services [#organizing-services]

Create one service file per resource type — \`resources.ts\`, \`entries.ts\`, \`webhooks.ts\`. Each file imports the shared client and exports functions that your application code can call. This makes it easy to find the code responsible for any given operation, and it lets you add resource-specific logic like validation or caching without cluttering other parts of the codebase.

Configuration [#configuration]

Keep your environment-specific settings organized and validated in a single configuration module.

Environment Variables [#environment-variables]

Store API keys, base URLs, and feature flags in environment variables and load them through a dedicated config file. This file should validate that all required variables are present at startup, so you get a clear error message immediately rather than a confusing failure deep in your application when a missing key causes an API call to fail.

Multi-Environment Setup [#multi-environment-setup]

For teams working across development, staging, and production environments, create separate \`.env\` files for each stage. Most frameworks support this natively with files like \`.env.local\` and \`.env.production\`. The SDK's \`environment\` option makes it easy to switch between the production and sandbox APIs based on your deployment target.
`,a={contents:[{heading:void 0,content:`As your integration grows beyond a single file, having a clear and consistent project structure becomes essential. The patterns described here represent what we've seen work well across hundreds of production integrations.`},{heading:`introduction`,content:`There's no single "correct" way to organize your code when using the Voltus SDK. The patterns here are recommendations based on real-world experience, not requirements. That said, following a consistent structure will make your codebase easier to navigate, test, and maintain — especially as your team grows and more people touch the integration code.`},{heading:`when-structure-matters`,content:`For a quick prototype or script, you can put everything in a single file and it'll work perfectly fine. But once your integration touches multiple resources, handles webhooks, manages caching, or runs in production with monitoring, having separate modules for each concern prevents your code from becoming tangled and hard to reason about.`},{heading:`the-recommended-layout`,content:"We suggest creating a dedicated directory for your Voltus integration — something like `src/voltus/` or `lib/voltus/`. Inside this directory, you'll have separate files for the client instance, each resource type's service layer, webhook handlers, and custom type definitions. This keeps all integration-related code in one place while maintaining clean separation between concerns."},{heading:`client-module`,content:`The most important architectural decision is where your client instance lives. A centralized client module ensures every part of your application uses the same configuration and connection pool.`},{heading:`single-source-of-truth`,content:"Create a file called `client.ts` that initializes the `VoltusClient` with your configuration and exports it. Every other module that needs to interact with the Voltus API imports the client from this one file. This means changes to your configuration — like updating the timeout or adding middleware — only need to happen in one place."},{heading:`avoiding-common-mistakes`,content:`The most common mistake we see is creating new client instances inline wherever they're needed. While this technically works, you end up with duplicated configuration scattered across your codebase, multiple connection pools competing for resources, and no central place to add cross-cutting concerns like logging or error handling. A single shared client instance solves all of these problems.`},{heading:`service-layer`,content:`Wrapping your SDK calls in service functions creates a clean separation between your API integration and the rest of your application logic.`},{heading:`why-use-services`,content:"Instead of calling `client.resources.list()` directly in your route handlers or React components, create functions like `getAllResources()` or `findResourceByName()` that encapsulate the business logic. Your route handler calls the service function, which handles pagination, caching, error recovery, and data transformation internally. The handler just gets back the data it needs."},{heading:`organizing-services`,content:"Create one service file per resource type — `resources.ts`, `entries.ts`, `webhooks.ts`. Each file imports the shared client and exports functions that your application code can call. This makes it easy to find the code responsible for any given operation, and it lets you add resource-specific logic like validation or caching without cluttering other parts of the codebase."},{heading:`configuration`,content:`Keep your environment-specific settings organized and validated in a single configuration module.`},{heading:`environment-variables`,content:`Store API keys, base URLs, and feature flags in environment variables and load them through a dedicated config file. This file should validate that all required variables are present at startup, so you get a clear error message immediately rather than a confusing failure deep in your application when a missing key causes an API call to fail.`},{heading:`multi-environment-setup`,content:"For teams working across development, staging, and production environments, create separate `.env` files for each stage. Most frameworks support this natively with files like `.env.local` and `.env.production`. The SDK's `environment` option makes it easy to switch between the production and sandbox APIs based on your deployment target."}],headings:[{id:`introduction`,content:`Introduction`},{id:`when-structure-matters`,content:`When Structure Matters`},{id:`the-recommended-layout`,content:`The Recommended Layout`},{id:`client-module`,content:`Client Module`},{id:`single-source-of-truth`,content:`Single Source of Truth`},{id:`avoiding-common-mistakes`,content:`Avoiding Common Mistakes`},{id:`service-layer`,content:`Service Layer`},{id:`why-use-services`,content:`Why Use Services`},{id:`organizing-services`,content:`Organizing Services`},{id:`configuration`,content:`Configuration`},{id:`environment-variables`,content:`Environment Variables`},{id:`multi-environment-setup`,content:`Multi-Environment Setup`}]},o=[{depth:2,url:`#introduction`,title:(0,n.jsx)(n.Fragment,{children:`Introduction`})},{depth:3,url:`#when-structure-matters`,title:(0,n.jsx)(n.Fragment,{children:`When Structure Matters`})},{depth:3,url:`#the-recommended-layout`,title:(0,n.jsx)(n.Fragment,{children:`The Recommended Layout`})},{depth:2,url:`#client-module`,title:(0,n.jsx)(n.Fragment,{children:`Client Module`})},{depth:3,url:`#single-source-of-truth`,title:(0,n.jsx)(n.Fragment,{children:`Single Source of Truth`})},{depth:3,url:`#avoiding-common-mistakes`,title:(0,n.jsx)(n.Fragment,{children:`Avoiding Common Mistakes`})},{depth:2,url:`#service-layer`,title:(0,n.jsx)(n.Fragment,{children:`Service Layer`})},{depth:3,url:`#why-use-services`,title:(0,n.jsx)(n.Fragment,{children:`Why Use Services`})},{depth:3,url:`#organizing-services`,title:(0,n.jsx)(n.Fragment,{children:`Organizing Services`})},{depth:2,url:`#configuration`,title:(0,n.jsx)(n.Fragment,{children:`Configuration`})},{depth:3,url:`#environment-variables`,title:(0,n.jsx)(n.Fragment,{children:`Environment Variables`})},{depth:3,url:`#multi-environment-setup`,title:(0,n.jsx)(n.Fragment,{children:`Multi-Environment Setup`})}];function s(e){let t={code:`code`,h2:`h2`,h3:`h3`,p:`p`,...e.components};return(0,n.jsxs)(n.Fragment,{children:[(0,n.jsx)(t.p,{children:`As your integration grows beyond a single file, having a clear and consistent project structure becomes essential. The patterns described here represent what we've seen work well across hundreds of production integrations.`}),`
`,(0,n.jsx)(t.h2,{id:`introduction`,children:`Introduction`}),`
`,(0,n.jsx)(t.p,{children:`There's no single "correct" way to organize your code when using the Voltus SDK. The patterns here are recommendations based on real-world experience, not requirements. That said, following a consistent structure will make your codebase easier to navigate, test, and maintain — especially as your team grows and more people touch the integration code.`}),`
`,(0,n.jsx)(t.h3,{id:`when-structure-matters`,children:`When Structure Matters`}),`
`,(0,n.jsx)(t.p,{children:`For a quick prototype or script, you can put everything in a single file and it'll work perfectly fine. But once your integration touches multiple resources, handles webhooks, manages caching, or runs in production with monitoring, having separate modules for each concern prevents your code from becoming tangled and hard to reason about.`}),`
`,(0,n.jsx)(t.h3,{id:`the-recommended-layout`,children:`The Recommended Layout`}),`
`,(0,n.jsxs)(t.p,{children:[`We suggest creating a dedicated directory for your Voltus integration — something like `,(0,n.jsx)(t.code,{children:`src/voltus/`}),` or `,(0,n.jsx)(t.code,{children:`lib/voltus/`}),`. Inside this directory, you'll have separate files for the client instance, each resource type's service layer, webhook handlers, and custom type definitions. This keeps all integration-related code in one place while maintaining clean separation between concerns.`]}),`
`,(0,n.jsx)(t.h2,{id:`client-module`,children:`Client Module`}),`
`,(0,n.jsx)(t.p,{children:`The most important architectural decision is where your client instance lives. A centralized client module ensures every part of your application uses the same configuration and connection pool.`}),`
`,(0,n.jsx)(t.h3,{id:`single-source-of-truth`,children:`Single Source of Truth`}),`
`,(0,n.jsxs)(t.p,{children:[`Create a file called `,(0,n.jsx)(t.code,{children:`client.ts`}),` that initializes the `,(0,n.jsx)(t.code,{children:`VoltusClient`}),` with your configuration and exports it. Every other module that needs to interact with the Voltus API imports the client from this one file. This means changes to your configuration — like updating the timeout or adding middleware — only need to happen in one place.`]}),`
`,(0,n.jsx)(t.h3,{id:`avoiding-common-mistakes`,children:`Avoiding Common Mistakes`}),`
`,(0,n.jsx)(t.p,{children:`The most common mistake we see is creating new client instances inline wherever they're needed. While this technically works, you end up with duplicated configuration scattered across your codebase, multiple connection pools competing for resources, and no central place to add cross-cutting concerns like logging or error handling. A single shared client instance solves all of these problems.`}),`
`,(0,n.jsx)(t.h2,{id:`service-layer`,children:`Service Layer`}),`
`,(0,n.jsx)(t.p,{children:`Wrapping your SDK calls in service functions creates a clean separation between your API integration and the rest of your application logic.`}),`
`,(0,n.jsx)(t.h3,{id:`why-use-services`,children:`Why Use Services`}),`
`,(0,n.jsxs)(t.p,{children:[`Instead of calling `,(0,n.jsx)(t.code,{children:`client.resources.list()`}),` directly in your route handlers or React components, create functions like `,(0,n.jsx)(t.code,{children:`getAllResources()`}),` or `,(0,n.jsx)(t.code,{children:`findResourceByName()`}),` that encapsulate the business logic. Your route handler calls the service function, which handles pagination, caching, error recovery, and data transformation internally. The handler just gets back the data it needs.`]}),`
`,(0,n.jsx)(t.h3,{id:`organizing-services`,children:`Organizing Services`}),`
`,(0,n.jsxs)(t.p,{children:[`Create one service file per resource type — `,(0,n.jsx)(t.code,{children:`resources.ts`}),`, `,(0,n.jsx)(t.code,{children:`entries.ts`}),`, `,(0,n.jsx)(t.code,{children:`webhooks.ts`}),`. Each file imports the shared client and exports functions that your application code can call. This makes it easy to find the code responsible for any given operation, and it lets you add resource-specific logic like validation or caching without cluttering other parts of the codebase.`]}),`
`,(0,n.jsx)(t.h2,{id:`configuration`,children:`Configuration`}),`
`,(0,n.jsx)(t.p,{children:`Keep your environment-specific settings organized and validated in a single configuration module.`}),`
`,(0,n.jsx)(t.h3,{id:`environment-variables`,children:`Environment Variables`}),`
`,(0,n.jsx)(t.p,{children:`Store API keys, base URLs, and feature flags in environment variables and load them through a dedicated config file. This file should validate that all required variables are present at startup, so you get a clear error message immediately rather than a confusing failure deep in your application when a missing key causes an API call to fail.`}),`
`,(0,n.jsx)(t.h3,{id:`multi-environment-setup`,children:`Multi-Environment Setup`}),`
`,(0,n.jsxs)(t.p,{children:[`For teams working across development, staging, and production environments, create separate `,(0,n.jsx)(t.code,{children:`.env`}),` files for each stage. Most frameworks support this natively with files like `,(0,n.jsx)(t.code,{children:`.env.local`}),` and `,(0,n.jsx)(t.code,{children:`.env.production`}),`. The SDK's `,(0,n.jsx)(t.code,{children:`environment`}),` option makes it easy to switch between the production and sandbox APIs based on your deployment target.`]})]})}function c(e={}){let{wrapper:t}=e.components||{};return t?(0,n.jsx)(t,{...e,children:(0,n.jsx)(s,{...e})}):s(e)}export{i as _markdown,c as default,r as frontmatter,a as structuredData,o as toc};