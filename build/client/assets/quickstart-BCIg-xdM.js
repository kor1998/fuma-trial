import{r as e}from"./chunk-0oqf_uz2.js";import{t}from"./jsx-runtime-CEHt124i.js";var n=e(t()),r={title:`Quick Start`,description:`Make your first API call with the Voltus SDK in under 5 minutes.`,icon:`Zap`},i=`

This guide will take you from zero to your first successful API call in just a few minutes. By the end, you'll have a working client, fetched live data from the Voltus platform, and created your first resource. No prior experience with the SDK is required.

Introduction [#introduction]

The Voltus SDK is designed to feel natural to JavaScript and TypeScript developers. If you've ever used a library like Stripe's SDK or the AWS SDK, the patterns will be immediately familiar. You create a client, call methods on it, and get back typed responses. Every method is async, every error is structured, and every response follows a consistent shape.

How It Works [#how-it-works]

Under the hood, the SDK wraps the Voltus REST API in a clean, type-safe interface. When you call \`client.resources.list()\`, the SDK constructs an HTTP request, sends it to the API, parses the response, and returns a fully typed JavaScript object. It also handles authentication headers, retry logic, rate limiting, and error parsing automatically.

What You'll Build [#what-youll-build]

By the end of this tutorial, you'll have a working script that connects to the Voltus API, retrieves a list of resources, creates a new resource, updates it, and then cleans up by deleting it. These four operations — list, create, update, delete — form the foundation of every integration.

Step 1 — Initialize the Client [#step-1--initialize-the-client]

The first thing every application needs is a configured client instance. This is the single object through which all your API interactions will flow.

Create the Client Module [#create-the-client-module]

Create a new file called \`client.ts\` in your project. Import the \`VoltusClient\` class from the SDK and create a new instance, passing your API key from the environment variable. Export this instance so other files can import and use it without creating their own clients.

Configuration Options [#configuration-options]

The client accepts several optional settings beyond the API key. You can set the \`environment\` to \`sandbox\` for testing, adjust the \`timeout\` for slow networks, or change the \`retries\` count for flaky connections. For now, the defaults are perfectly fine — you can always adjust them later as your needs evolve.

Step 2 — Fetch Resources [#step-2--fetch-resources]

With the client ready, you can start reading data from the platform. List operations are the most common starting point for any integration.

Making the Request [#making-the-request]

Call \`client.resources.list()\` to fetch the first page of resources. The response object contains two properties — \`data\`, which is an array of resource objects, and \`pagination\`, which tells you the total count, current page size, and whether more pages are available. Each resource in the array has an \`id\`, \`name\`, \`description\`, \`status\`, \`tags\`, and timestamp fields.

Understanding the Response [#understanding-the-response]

The pagination object follows a consistent pattern across every list endpoint in the SDK. The \`total\` field tells you how many records exist in total, \`limit\` is how many were returned in this page, \`offset\` is where this page started, and \`hasMore\` indicates whether there are additional pages to fetch. This consistent structure means you only need to learn the pattern once.

Step 3 — Create a Resource [#step-3--create-a-resource]

Reading data is useful, but most integrations also need to write data back to the platform. Creating a resource demonstrates the SDK's mutation capabilities.

Building the Payload [#building-the-payload]

Call \`client.resources.create()\` with an object containing the fields for your new resource. The only required field is \`name\` — everything else is optional. You can include a \`description\`, an array of \`tags\` for organization, and a \`metadata\` object for storing arbitrary key-value pairs that are specific to your application.

Handling the Response [#handling-the-response]

The create method returns the newly created resource with all of its server-generated fields populated — including the unique \`id\`, the \`status\` which defaults to \`draft\`, and the \`createdAt\` and \`updatedAt\` timestamps. You can use the returned \`id\` immediately for subsequent operations like updates or deletions.

Step 4 — Update and Delete [#step-4--update-and-delete]

The final operations complete the CRUD lifecycle. Updating lets you modify existing resources, and deleting removes them permanently.

Updating Fields [#updating-fields]

Call \`client.resources.update()\` with the resource ID and an object containing only the fields you want to change. You don't need to send the entire resource — the SDK uses PATCH semantics, so only the specified fields are modified and everything else remains unchanged. The method returns the full updated resource so you can verify the changes.

Deleting a Resource [#deleting-a-resource]

Call \`client.resources.delete()\` with just the resource ID. The method returns void on success and throws a \`NotFoundError\` if the resource doesn't exist. Deletion is permanent and cannot be undone, so make sure you're targeting the correct resource before calling this method in production code.

Next Steps [#next-steps]

Now that you've completed the basic CRUD operations, you have a solid foundation for building any integration with the Voltus platform.

Recommended Reading [#recommended-reading]

Head to the Authentication guide to learn about API key scopes, OAuth flows, and token management. Then check out the Error Handling page to understand how to gracefully handle failures in production. Finally, the Guides section covers real-world patterns like pagination, caching, and rate limit management that will help you scale your integration.

Join the Community [#join-the-community]

If you have questions or want to share what you're building, join our community Discord where other developers discuss patterns, share code snippets, and help each other troubleshoot issues. We also love to see what people build with the SDK, so feel free to tag us on Twitter with your projects.
`,a={contents:[{heading:void 0,content:`This guide will take you from zero to your first successful API call in just a few minutes. By the end, you'll have a working client, fetched live data from the Voltus platform, and created your first resource. No prior experience with the SDK is required.`},{heading:`introduction`,content:`The Voltus SDK is designed to feel natural to JavaScript and TypeScript developers. If you've ever used a library like Stripe's SDK or the AWS SDK, the patterns will be immediately familiar. You create a client, call methods on it, and get back typed responses. Every method is async, every error is structured, and every response follows a consistent shape.`},{heading:`how-it-works`,content:"Under the hood, the SDK wraps the Voltus REST API in a clean, type-safe interface. When you call `client.resources.list()`, the SDK constructs an HTTP request, sends it to the API, parses the response, and returns a fully typed JavaScript object. It also handles authentication headers, retry logic, rate limiting, and error parsing automatically."},{heading:`what-youll-build`,content:`By the end of this tutorial, you'll have a working script that connects to the Voltus API, retrieves a list of resources, creates a new resource, updates it, and then cleans up by deleting it. These four operations — list, create, update, delete — form the foundation of every integration.`},{heading:`step-1--initialize-the-client`,content:`The first thing every application needs is a configured client instance. This is the single object through which all your API interactions will flow.`},{heading:`create-the-client-module`,content:"Create a new file called `client.ts` in your project. Import the `VoltusClient` class from the SDK and create a new instance, passing your API key from the environment variable. Export this instance so other files can import and use it without creating their own clients."},{heading:`configuration-options`,content:"The client accepts several optional settings beyond the API key. You can set the `environment` to `sandbox` for testing, adjust the `timeout` for slow networks, or change the `retries` count for flaky connections. For now, the defaults are perfectly fine — you can always adjust them later as your needs evolve."},{heading:`step-2--fetch-resources`,content:`With the client ready, you can start reading data from the platform. List operations are the most common starting point for any integration.`},{heading:`making-the-request`,content:"Call `client.resources.list()` to fetch the first page of resources. The response object contains two properties — `data`, which is an array of resource objects, and `pagination`, which tells you the total count, current page size, and whether more pages are available. Each resource in the array has an `id`, `name`, `description`, `status`, `tags`, and timestamp fields."},{heading:`understanding-the-response`,content:"The pagination object follows a consistent pattern across every list endpoint in the SDK. The `total` field tells you how many records exist in total, `limit` is how many were returned in this page, `offset` is where this page started, and `hasMore` indicates whether there are additional pages to fetch. This consistent structure means you only need to learn the pattern once."},{heading:`step-3--create-a-resource`,content:`Reading data is useful, but most integrations also need to write data back to the platform. Creating a resource demonstrates the SDK's mutation capabilities.`},{heading:`building-the-payload`,content:"Call `client.resources.create()` with an object containing the fields for your new resource. The only required field is `name` — everything else is optional. You can include a `description`, an array of `tags` for organization, and a `metadata` object for storing arbitrary key-value pairs that are specific to your application."},{heading:`handling-the-response`,content:"The create method returns the newly created resource with all of its server-generated fields populated — including the unique `id`, the `status` which defaults to `draft`, and the `createdAt` and `updatedAt` timestamps. You can use the returned `id` immediately for subsequent operations like updates or deletions."},{heading:`step-4--update-and-delete`,content:`The final operations complete the CRUD lifecycle. Updating lets you modify existing resources, and deleting removes them permanently.`},{heading:`updating-fields`,content:"Call `client.resources.update()` with the resource ID and an object containing only the fields you want to change. You don't need to send the entire resource — the SDK uses PATCH semantics, so only the specified fields are modified and everything else remains unchanged. The method returns the full updated resource so you can verify the changes."},{heading:`deleting-a-resource`,content:"Call `client.resources.delete()` with just the resource ID. The method returns void on success and throws a `NotFoundError` if the resource doesn't exist. Deletion is permanent and cannot be undone, so make sure you're targeting the correct resource before calling this method in production code."},{heading:`next-steps`,content:`Now that you've completed the basic CRUD operations, you have a solid foundation for building any integration with the Voltus platform.`},{heading:`recommended-reading`,content:`Head to the Authentication guide to learn about API key scopes, OAuth flows, and token management. Then check out the Error Handling page to understand how to gracefully handle failures in production. Finally, the Guides section covers real-world patterns like pagination, caching, and rate limit management that will help you scale your integration.`},{heading:`join-the-community`,content:`If you have questions or want to share what you're building, join our community Discord where other developers discuss patterns, share code snippets, and help each other troubleshoot issues. We also love to see what people build with the SDK, so feel free to tag us on Twitter with your projects.`}],headings:[{id:`introduction`,content:`Introduction`},{id:`how-it-works`,content:`How It Works`},{id:`what-youll-build`,content:`What You'll Build`},{id:`step-1--initialize-the-client`,content:`Step 1 — Initialize the Client`},{id:`create-the-client-module`,content:`Create the Client Module`},{id:`configuration-options`,content:`Configuration Options`},{id:`step-2--fetch-resources`,content:`Step 2 — Fetch Resources`},{id:`making-the-request`,content:`Making the Request`},{id:`understanding-the-response`,content:`Understanding the Response`},{id:`step-3--create-a-resource`,content:`Step 3 — Create a Resource`},{id:`building-the-payload`,content:`Building the Payload`},{id:`handling-the-response`,content:`Handling the Response`},{id:`step-4--update-and-delete`,content:`Step 4 — Update and Delete`},{id:`updating-fields`,content:`Updating Fields`},{id:`deleting-a-resource`,content:`Deleting a Resource`},{id:`next-steps`,content:`Next Steps`},{id:`recommended-reading`,content:`Recommended Reading`},{id:`join-the-community`,content:`Join the Community`}]},o=[{depth:2,url:`#introduction`,title:(0,n.jsx)(n.Fragment,{children:`Introduction`})},{depth:3,url:`#how-it-works`,title:(0,n.jsx)(n.Fragment,{children:`How It Works`})},{depth:3,url:`#what-youll-build`,title:(0,n.jsx)(n.Fragment,{children:`What You'll Build`})},{depth:2,url:`#step-1--initialize-the-client`,title:(0,n.jsx)(n.Fragment,{children:`Step 1 — Initialize the Client`})},{depth:3,url:`#create-the-client-module`,title:(0,n.jsx)(n.Fragment,{children:`Create the Client Module`})},{depth:3,url:`#configuration-options`,title:(0,n.jsx)(n.Fragment,{children:`Configuration Options`})},{depth:2,url:`#step-2--fetch-resources`,title:(0,n.jsx)(n.Fragment,{children:`Step 2 — Fetch Resources`})},{depth:3,url:`#making-the-request`,title:(0,n.jsx)(n.Fragment,{children:`Making the Request`})},{depth:3,url:`#understanding-the-response`,title:(0,n.jsx)(n.Fragment,{children:`Understanding the Response`})},{depth:2,url:`#step-3--create-a-resource`,title:(0,n.jsx)(n.Fragment,{children:`Step 3 — Create a Resource`})},{depth:3,url:`#building-the-payload`,title:(0,n.jsx)(n.Fragment,{children:`Building the Payload`})},{depth:3,url:`#handling-the-response`,title:(0,n.jsx)(n.Fragment,{children:`Handling the Response`})},{depth:2,url:`#step-4--update-and-delete`,title:(0,n.jsx)(n.Fragment,{children:`Step 4 — Update and Delete`})},{depth:3,url:`#updating-fields`,title:(0,n.jsx)(n.Fragment,{children:`Updating Fields`})},{depth:3,url:`#deleting-a-resource`,title:(0,n.jsx)(n.Fragment,{children:`Deleting a Resource`})},{depth:2,url:`#next-steps`,title:(0,n.jsx)(n.Fragment,{children:`Next Steps`})},{depth:3,url:`#recommended-reading`,title:(0,n.jsx)(n.Fragment,{children:`Recommended Reading`})},{depth:3,url:`#join-the-community`,title:(0,n.jsx)(n.Fragment,{children:`Join the Community`})}];function s(e){let t={code:`code`,h2:`h2`,h3:`h3`,p:`p`,...e.components};return(0,n.jsxs)(n.Fragment,{children:[(0,n.jsx)(t.p,{children:`This guide will take you from zero to your first successful API call in just a few minutes. By the end, you'll have a working client, fetched live data from the Voltus platform, and created your first resource. No prior experience with the SDK is required.`}),`
`,(0,n.jsx)(t.h2,{id:`introduction`,children:`Introduction`}),`
`,(0,n.jsx)(t.p,{children:`The Voltus SDK is designed to feel natural to JavaScript and TypeScript developers. If you've ever used a library like Stripe's SDK or the AWS SDK, the patterns will be immediately familiar. You create a client, call methods on it, and get back typed responses. Every method is async, every error is structured, and every response follows a consistent shape.`}),`
`,(0,n.jsx)(t.h3,{id:`how-it-works`,children:`How It Works`}),`
`,(0,n.jsxs)(t.p,{children:[`Under the hood, the SDK wraps the Voltus REST API in a clean, type-safe interface. When you call `,(0,n.jsx)(t.code,{children:`client.resources.list()`}),`, the SDK constructs an HTTP request, sends it to the API, parses the response, and returns a fully typed JavaScript object. It also handles authentication headers, retry logic, rate limiting, and error parsing automatically.`]}),`
`,(0,n.jsx)(t.h3,{id:`what-youll-build`,children:`What You'll Build`}),`
`,(0,n.jsx)(t.p,{children:`By the end of this tutorial, you'll have a working script that connects to the Voltus API, retrieves a list of resources, creates a new resource, updates it, and then cleans up by deleting it. These four operations — list, create, update, delete — form the foundation of every integration.`}),`
`,(0,n.jsx)(t.h2,{id:`step-1--initialize-the-client`,children:`Step 1 — Initialize the Client`}),`
`,(0,n.jsx)(t.p,{children:`The first thing every application needs is a configured client instance. This is the single object through which all your API interactions will flow.`}),`
`,(0,n.jsx)(t.h3,{id:`create-the-client-module`,children:`Create the Client Module`}),`
`,(0,n.jsxs)(t.p,{children:[`Create a new file called `,(0,n.jsx)(t.code,{children:`client.ts`}),` in your project. Import the `,(0,n.jsx)(t.code,{children:`VoltusClient`}),` class from the SDK and create a new instance, passing your API key from the environment variable. Export this instance so other files can import and use it without creating their own clients.`]}),`
`,(0,n.jsx)(t.h3,{id:`configuration-options`,children:`Configuration Options`}),`
`,(0,n.jsxs)(t.p,{children:[`The client accepts several optional settings beyond the API key. You can set the `,(0,n.jsx)(t.code,{children:`environment`}),` to `,(0,n.jsx)(t.code,{children:`sandbox`}),` for testing, adjust the `,(0,n.jsx)(t.code,{children:`timeout`}),` for slow networks, or change the `,(0,n.jsx)(t.code,{children:`retries`}),` count for flaky connections. For now, the defaults are perfectly fine — you can always adjust them later as your needs evolve.`]}),`
`,(0,n.jsx)(t.h2,{id:`step-2--fetch-resources`,children:`Step 2 — Fetch Resources`}),`
`,(0,n.jsx)(t.p,{children:`With the client ready, you can start reading data from the platform. List operations are the most common starting point for any integration.`}),`
`,(0,n.jsx)(t.h3,{id:`making-the-request`,children:`Making the Request`}),`
`,(0,n.jsxs)(t.p,{children:[`Call `,(0,n.jsx)(t.code,{children:`client.resources.list()`}),` to fetch the first page of resources. The response object contains two properties — `,(0,n.jsx)(t.code,{children:`data`}),`, which is an array of resource objects, and `,(0,n.jsx)(t.code,{children:`pagination`}),`, which tells you the total count, current page size, and whether more pages are available. Each resource in the array has an `,(0,n.jsx)(t.code,{children:`id`}),`, `,(0,n.jsx)(t.code,{children:`name`}),`, `,(0,n.jsx)(t.code,{children:`description`}),`, `,(0,n.jsx)(t.code,{children:`status`}),`, `,(0,n.jsx)(t.code,{children:`tags`}),`, and timestamp fields.`]}),`
`,(0,n.jsx)(t.h3,{id:`understanding-the-response`,children:`Understanding the Response`}),`
`,(0,n.jsxs)(t.p,{children:[`The pagination object follows a consistent pattern across every list endpoint in the SDK. The `,(0,n.jsx)(t.code,{children:`total`}),` field tells you how many records exist in total, `,(0,n.jsx)(t.code,{children:`limit`}),` is how many were returned in this page, `,(0,n.jsx)(t.code,{children:`offset`}),` is where this page started, and `,(0,n.jsx)(t.code,{children:`hasMore`}),` indicates whether there are additional pages to fetch. This consistent structure means you only need to learn the pattern once.`]}),`
`,(0,n.jsx)(t.h2,{id:`step-3--create-a-resource`,children:`Step 3 — Create a Resource`}),`
`,(0,n.jsx)(t.p,{children:`Reading data is useful, but most integrations also need to write data back to the platform. Creating a resource demonstrates the SDK's mutation capabilities.`}),`
`,(0,n.jsx)(t.h3,{id:`building-the-payload`,children:`Building the Payload`}),`
`,(0,n.jsxs)(t.p,{children:[`Call `,(0,n.jsx)(t.code,{children:`client.resources.create()`}),` with an object containing the fields for your new resource. The only required field is `,(0,n.jsx)(t.code,{children:`name`}),` — everything else is optional. You can include a `,(0,n.jsx)(t.code,{children:`description`}),`, an array of `,(0,n.jsx)(t.code,{children:`tags`}),` for organization, and a `,(0,n.jsx)(t.code,{children:`metadata`}),` object for storing arbitrary key-value pairs that are specific to your application.`]}),`
`,(0,n.jsx)(t.h3,{id:`handling-the-response`,children:`Handling the Response`}),`
`,(0,n.jsxs)(t.p,{children:[`The create method returns the newly created resource with all of its server-generated fields populated — including the unique `,(0,n.jsx)(t.code,{children:`id`}),`, the `,(0,n.jsx)(t.code,{children:`status`}),` which defaults to `,(0,n.jsx)(t.code,{children:`draft`}),`, and the `,(0,n.jsx)(t.code,{children:`createdAt`}),` and `,(0,n.jsx)(t.code,{children:`updatedAt`}),` timestamps. You can use the returned `,(0,n.jsx)(t.code,{children:`id`}),` immediately for subsequent operations like updates or deletions.`]}),`
`,(0,n.jsx)(t.h2,{id:`step-4--update-and-delete`,children:`Step 4 — Update and Delete`}),`
`,(0,n.jsx)(t.p,{children:`The final operations complete the CRUD lifecycle. Updating lets you modify existing resources, and deleting removes them permanently.`}),`
`,(0,n.jsx)(t.h3,{id:`updating-fields`,children:`Updating Fields`}),`
`,(0,n.jsxs)(t.p,{children:[`Call `,(0,n.jsx)(t.code,{children:`client.resources.update()`}),` with the resource ID and an object containing only the fields you want to change. You don't need to send the entire resource — the SDK uses PATCH semantics, so only the specified fields are modified and everything else remains unchanged. The method returns the full updated resource so you can verify the changes.`]}),`
`,(0,n.jsx)(t.h3,{id:`deleting-a-resource`,children:`Deleting a Resource`}),`
`,(0,n.jsxs)(t.p,{children:[`Call `,(0,n.jsx)(t.code,{children:`client.resources.delete()`}),` with just the resource ID. The method returns void on success and throws a `,(0,n.jsx)(t.code,{children:`NotFoundError`}),` if the resource doesn't exist. Deletion is permanent and cannot be undone, so make sure you're targeting the correct resource before calling this method in production code.`]}),`
`,(0,n.jsx)(t.h2,{id:`next-steps`,children:`Next Steps`}),`
`,(0,n.jsx)(t.p,{children:`Now that you've completed the basic CRUD operations, you have a solid foundation for building any integration with the Voltus platform.`}),`
`,(0,n.jsx)(t.h3,{id:`recommended-reading`,children:`Recommended Reading`}),`
`,(0,n.jsx)(t.p,{children:`Head to the Authentication guide to learn about API key scopes, OAuth flows, and token management. Then check out the Error Handling page to understand how to gracefully handle failures in production. Finally, the Guides section covers real-world patterns like pagination, caching, and rate limit management that will help you scale your integration.`}),`
`,(0,n.jsx)(t.h3,{id:`join-the-community`,children:`Join the Community`}),`
`,(0,n.jsx)(t.p,{children:`If you have questions or want to share what you're building, join our community Discord where other developers discuss patterns, share code snippets, and help each other troubleshoot issues. We also love to see what people build with the SDK, so feel free to tag us on Twitter with your projects.`})]})}function c(e={}){let{wrapper:t}=e.components||{};return t?(0,n.jsx)(t,{...e,children:(0,n.jsx)(s,{...e})}):s(e)}export{i as _markdown,c as default,r as frontmatter,a as structuredData,o as toc};