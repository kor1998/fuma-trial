import{r as e}from"./chunk-0oqf_uz2.js";import{t}from"./jsx-runtime-CEHt124i.js";var n=e(t()),r={title:`Error Handling`,description:`Gracefully handle errors and edge cases in your Voltus SDK integration.`,icon:`AlertCircle`},i=`

Every API call has the potential to fail. The Voltus SDK is designed to make error handling as predictable and informative as possible, with structured error types that tell you exactly what went wrong and whether it's safe to retry.

Introduction [#introduction]

Instead of throwing generic JavaScript errors with vague messages, the SDK provides a hierarchy of typed error classes. Each class corresponds to a specific category of failure and carries rich metadata for debugging.

Error Philosophy [#error-philosophy]

We believe errors should be actionable. Every error thrown by the SDK includes a machine-readable code for programmatic handling, a human-readable message for logging, a request ID for support inquiries, and a \`retryable\` flag that tells you whether retrying is worthwhile.

When Errors Occur [#when-errors-occur]

Errors can happen for many reasons — invalid credentials, missing resources, malformed input, rate limiting, network issues, or server-side problems. The SDK categorizes each scenario into a specific error type so you can handle them precisely rather than catching everything with a generic handler.

Error Types [#error-types]

The SDK defines a hierarchy of error classes, all extending from a base \`VoltusError\` class. This lets you catch all SDK errors broadly or target specific failure types.

Client Errors [#client-errors]

\`AuthenticationError\` fires when your API key is invalid or expired. \`AuthorizationError\` fires when your key lacks the required scope for the operation. \`NotFoundError\` fires when you reference a resource that doesn't exist. \`ValidationError\` fires when your input data fails server-side validation and includes a \`fields\` array describing each invalid field.

Server Errors [#server-errors]

\`RateLimitError\` fires when you've exceeded your API quota and includes a \`retryAfter\` property indicating how long to wait. \`ServerError\` fires for unexpected server-side failures and is always safe to retry. The SDK's built-in retry mechanism handles both of these automatically, but you can also catch them explicitly for custom handling.

Recovery Patterns [#recovery-patterns]

The most robust applications don't just catch errors — they recover from them gracefully using established patterns.

Fallback Strategy [#fallback-strategy]

For non-critical operations, implement a fallback that returns cached data or a default value when an API call fails. This keeps your application functional even during temporary outages. The pattern is simple — wrap your API call in a try-catch and return the fallback value in the catch block.

Dead Letter Queue [#dead-letter-queue]

For operations that must eventually succeed, log failed requests with full context and move on to the next item. A separate process can retry failed operations with longer delays, or flag them for manual review. This ensures one bad record doesn't block the processing of everything behind it.

Global Error Handling [#global-error-handling]

For cross-cutting concerns like monitoring and alerting, the SDK supports a global error handler that fires on every failure.

Setting Up Monitoring [#setting-up-monitoring]

Call \`client.onError()\` with a callback function that receives the full error object. This is the ideal place to send errors to services like Sentry or Datadog — the handler receives all metadata including the request ID, error code, and endpoint, so you can automatically enrich your error reports.

Building Dashboards [#building-dashboards]

With structured error types flowing into your monitoring system, you can build dashboards that break down failures by type. This shows you at a glance whether you're experiencing authentication issues, hitting rate limits, or dealing with server-side problems — giving you the observability needed to maintain a healthy production integration.
`,a={contents:[{heading:void 0,content:`Every API call has the potential to fail. The Voltus SDK is designed to make error handling as predictable and informative as possible, with structured error types that tell you exactly what went wrong and whether it's safe to retry.`},{heading:`introduction`,content:`Instead of throwing generic JavaScript errors with vague messages, the SDK provides a hierarchy of typed error classes. Each class corresponds to a specific category of failure and carries rich metadata for debugging.`},{heading:`error-philosophy`,content:"We believe errors should be actionable. Every error thrown by the SDK includes a machine-readable code for programmatic handling, a human-readable message for logging, a request ID for support inquiries, and a `retryable` flag that tells you whether retrying is worthwhile."},{heading:`when-errors-occur`,content:`Errors can happen for many reasons — invalid credentials, missing resources, malformed input, rate limiting, network issues, or server-side problems. The SDK categorizes each scenario into a specific error type so you can handle them precisely rather than catching everything with a generic handler.`},{heading:`error-types`,content:"The SDK defines a hierarchy of error classes, all extending from a base `VoltusError` class. This lets you catch all SDK errors broadly or target specific failure types."},{heading:`client-errors`,content:"`AuthenticationError` fires when your API key is invalid or expired. `AuthorizationError` fires when your key lacks the required scope for the operation. `NotFoundError` fires when you reference a resource that doesn't exist. `ValidationError` fires when your input data fails server-side validation and includes a `fields` array describing each invalid field."},{heading:`server-errors`,content:"`RateLimitError` fires when you've exceeded your API quota and includes a `retryAfter` property indicating how long to wait. `ServerError` fires for unexpected server-side failures and is always safe to retry. The SDK's built-in retry mechanism handles both of these automatically, but you can also catch them explicitly for custom handling."},{heading:`recovery-patterns`,content:`The most robust applications don't just catch errors — they recover from them gracefully using established patterns.`},{heading:`fallback-strategy`,content:`For non-critical operations, implement a fallback that returns cached data or a default value when an API call fails. This keeps your application functional even during temporary outages. The pattern is simple — wrap your API call in a try-catch and return the fallback value in the catch block.`},{heading:`dead-letter-queue`,content:`For operations that must eventually succeed, log failed requests with full context and move on to the next item. A separate process can retry failed operations with longer delays, or flag them for manual review. This ensures one bad record doesn't block the processing of everything behind it.`},{heading:`global-error-handling`,content:`For cross-cutting concerns like monitoring and alerting, the SDK supports a global error handler that fires on every failure.`},{heading:`setting-up-monitoring`,content:"Call `client.onError()` with a callback function that receives the full error object. This is the ideal place to send errors to services like Sentry or Datadog — the handler receives all metadata including the request ID, error code, and endpoint, so you can automatically enrich your error reports."},{heading:`building-dashboards`,content:`With structured error types flowing into your monitoring system, you can build dashboards that break down failures by type. This shows you at a glance whether you're experiencing authentication issues, hitting rate limits, or dealing with server-side problems — giving you the observability needed to maintain a healthy production integration.`}],headings:[{id:`introduction`,content:`Introduction`},{id:`error-philosophy`,content:`Error Philosophy`},{id:`when-errors-occur`,content:`When Errors Occur`},{id:`error-types`,content:`Error Types`},{id:`client-errors`,content:`Client Errors`},{id:`server-errors`,content:`Server Errors`},{id:`recovery-patterns`,content:`Recovery Patterns`},{id:`fallback-strategy`,content:`Fallback Strategy`},{id:`dead-letter-queue`,content:`Dead Letter Queue`},{id:`global-error-handling`,content:`Global Error Handling`},{id:`setting-up-monitoring`,content:`Setting Up Monitoring`},{id:`building-dashboards`,content:`Building Dashboards`}]},o=[{depth:2,url:`#introduction`,title:(0,n.jsx)(n.Fragment,{children:`Introduction`})},{depth:3,url:`#error-philosophy`,title:(0,n.jsx)(n.Fragment,{children:`Error Philosophy`})},{depth:3,url:`#when-errors-occur`,title:(0,n.jsx)(n.Fragment,{children:`When Errors Occur`})},{depth:2,url:`#error-types`,title:(0,n.jsx)(n.Fragment,{children:`Error Types`})},{depth:3,url:`#client-errors`,title:(0,n.jsx)(n.Fragment,{children:`Client Errors`})},{depth:3,url:`#server-errors`,title:(0,n.jsx)(n.Fragment,{children:`Server Errors`})},{depth:2,url:`#recovery-patterns`,title:(0,n.jsx)(n.Fragment,{children:`Recovery Patterns`})},{depth:3,url:`#fallback-strategy`,title:(0,n.jsx)(n.Fragment,{children:`Fallback Strategy`})},{depth:3,url:`#dead-letter-queue`,title:(0,n.jsx)(n.Fragment,{children:`Dead Letter Queue`})},{depth:2,url:`#global-error-handling`,title:(0,n.jsx)(n.Fragment,{children:`Global Error Handling`})},{depth:3,url:`#setting-up-monitoring`,title:(0,n.jsx)(n.Fragment,{children:`Setting Up Monitoring`})},{depth:3,url:`#building-dashboards`,title:(0,n.jsx)(n.Fragment,{children:`Building Dashboards`})}];function s(e){let t={code:`code`,h2:`h2`,h3:`h3`,p:`p`,...e.components};return(0,n.jsxs)(n.Fragment,{children:[(0,n.jsx)(t.p,{children:`Every API call has the potential to fail. The Voltus SDK is designed to make error handling as predictable and informative as possible, with structured error types that tell you exactly what went wrong and whether it's safe to retry.`}),`
`,(0,n.jsx)(t.h2,{id:`introduction`,children:`Introduction`}),`
`,(0,n.jsx)(t.p,{children:`Instead of throwing generic JavaScript errors with vague messages, the SDK provides a hierarchy of typed error classes. Each class corresponds to a specific category of failure and carries rich metadata for debugging.`}),`
`,(0,n.jsx)(t.h3,{id:`error-philosophy`,children:`Error Philosophy`}),`
`,(0,n.jsxs)(t.p,{children:[`We believe errors should be actionable. Every error thrown by the SDK includes a machine-readable code for programmatic handling, a human-readable message for logging, a request ID for support inquiries, and a `,(0,n.jsx)(t.code,{children:`retryable`}),` flag that tells you whether retrying is worthwhile.`]}),`
`,(0,n.jsx)(t.h3,{id:`when-errors-occur`,children:`When Errors Occur`}),`
`,(0,n.jsx)(t.p,{children:`Errors can happen for many reasons — invalid credentials, missing resources, malformed input, rate limiting, network issues, or server-side problems. The SDK categorizes each scenario into a specific error type so you can handle them precisely rather than catching everything with a generic handler.`}),`
`,(0,n.jsx)(t.h2,{id:`error-types`,children:`Error Types`}),`
`,(0,n.jsxs)(t.p,{children:[`The SDK defines a hierarchy of error classes, all extending from a base `,(0,n.jsx)(t.code,{children:`VoltusError`}),` class. This lets you catch all SDK errors broadly or target specific failure types.`]}),`
`,(0,n.jsx)(t.h3,{id:`client-errors`,children:`Client Errors`}),`
`,(0,n.jsxs)(t.p,{children:[(0,n.jsx)(t.code,{children:`AuthenticationError`}),` fires when your API key is invalid or expired. `,(0,n.jsx)(t.code,{children:`AuthorizationError`}),` fires when your key lacks the required scope for the operation. `,(0,n.jsx)(t.code,{children:`NotFoundError`}),` fires when you reference a resource that doesn't exist. `,(0,n.jsx)(t.code,{children:`ValidationError`}),` fires when your input data fails server-side validation and includes a `,(0,n.jsx)(t.code,{children:`fields`}),` array describing each invalid field.`]}),`
`,(0,n.jsx)(t.h3,{id:`server-errors`,children:`Server Errors`}),`
`,(0,n.jsxs)(t.p,{children:[(0,n.jsx)(t.code,{children:`RateLimitError`}),` fires when you've exceeded your API quota and includes a `,(0,n.jsx)(t.code,{children:`retryAfter`}),` property indicating how long to wait. `,(0,n.jsx)(t.code,{children:`ServerError`}),` fires for unexpected server-side failures and is always safe to retry. The SDK's built-in retry mechanism handles both of these automatically, but you can also catch them explicitly for custom handling.`]}),`
`,(0,n.jsx)(t.h2,{id:`recovery-patterns`,children:`Recovery Patterns`}),`
`,(0,n.jsx)(t.p,{children:`The most robust applications don't just catch errors — they recover from them gracefully using established patterns.`}),`
`,(0,n.jsx)(t.h3,{id:`fallback-strategy`,children:`Fallback Strategy`}),`
`,(0,n.jsx)(t.p,{children:`For non-critical operations, implement a fallback that returns cached data or a default value when an API call fails. This keeps your application functional even during temporary outages. The pattern is simple — wrap your API call in a try-catch and return the fallback value in the catch block.`}),`
`,(0,n.jsx)(t.h3,{id:`dead-letter-queue`,children:`Dead Letter Queue`}),`
`,(0,n.jsx)(t.p,{children:`For operations that must eventually succeed, log failed requests with full context and move on to the next item. A separate process can retry failed operations with longer delays, or flag them for manual review. This ensures one bad record doesn't block the processing of everything behind it.`}),`
`,(0,n.jsx)(t.h2,{id:`global-error-handling`,children:`Global Error Handling`}),`
`,(0,n.jsx)(t.p,{children:`For cross-cutting concerns like monitoring and alerting, the SDK supports a global error handler that fires on every failure.`}),`
`,(0,n.jsx)(t.h3,{id:`setting-up-monitoring`,children:`Setting Up Monitoring`}),`
`,(0,n.jsxs)(t.p,{children:[`Call `,(0,n.jsx)(t.code,{children:`client.onError()`}),` with a callback function that receives the full error object. This is the ideal place to send errors to services like Sentry or Datadog — the handler receives all metadata including the request ID, error code, and endpoint, so you can automatically enrich your error reports.`]}),`
`,(0,n.jsx)(t.h3,{id:`building-dashboards`,children:`Building Dashboards`}),`
`,(0,n.jsx)(t.p,{children:`With structured error types flowing into your monitoring system, you can build dashboards that break down failures by type. This shows you at a glance whether you're experiencing authentication issues, hitting rate limits, or dealing with server-side problems — giving you the observability needed to maintain a healthy production integration.`})]})}function c(e={}){let{wrapper:t}=e.components||{};return t?(0,n.jsx)(t,{...e,children:(0,n.jsx)(s,{...e})}):s(e)}export{i as _markdown,c as default,r as frontmatter,a as structuredData,o as toc};