import{r as e}from"./chunk-0oqf_uz2.js";import{t}from"./jsx-runtime-CEHt124i.js";var n=e(t()),r={title:`Authentication`,description:`Learn how to authenticate with the Voltus API using API keys and OAuth tokens.`,icon:`Lock`},i=`

Authentication is the foundation of every interaction with the Voltus platform. Before your application can read or write any data, it needs to prove its identity. The SDK makes this seamless — you pass your credentials once during client initialization, and every subsequent request is automatically authenticated.

Introduction [#introduction]

The Voltus API supports two authentication methods — API keys for server-side applications and OAuth 2.0 for user-facing integrations. Both methods are fully supported by the SDK, and you can switch between them without changing any of your data-fetching code.

Choosing a Method [#choosing-a-method]

If your application runs on a server and acts with its own identity, use an API key. If your application acts on behalf of individual users who need to grant permission, use OAuth 2.0. Most integrations start with API keys because they're simpler to set up, and migrate to OAuth later if user-level authentication becomes necessary.

Security Principles [#security-principles]

Regardless of which method you use, never expose credentials in client-side code, version control, or logs. Treat your API key like a password — store it in environment variables, rotate it periodically, and revoke it immediately if it's ever compromised.

API Keys [#api-keys]

API keys are the simplest and most common way to authenticate with the Voltus API. You generate a key in the dashboard, pass it to the SDK, and you're ready to make requests.

Key Types [#key-types]

Voltus issues three types of API keys. Secret keys start with \`vlt_sk_\` and grant full read-write access — use these only on the server. Public keys start with \`vlt_pk_\` and are restricted to read-only operations — these are safe for client-side code. Test keys start with \`vlt_test_\` and only work against the sandbox environment — use these during development.

Using Keys in the SDK [#using-keys-in-the-sdk]

Pass your API key to the \`VoltusClient\` constructor and the SDK handles everything else. It attaches the key as a Bearer token in the Authorization header of every request, validates the key format at initialization time, and throws a clear error if the key is malformed or missing.

OAuth 2.0 [#oauth-20]

For applications that act on behalf of users, the SDK supports the full OAuth 2.0 authorization code flow with automatic token management.

The Authorization Flow [#the-authorization-flow]

First, generate an authorization URL using \`client.auth.getAuthorizationUrl()\` with the scopes your application needs. Redirect the user to this URL — they'll see a consent screen showing what permissions your app is requesting. After they approve, Voltus redirects back to your callback URL with an authorization code that you exchange for access tokens.

Token Lifecycle [#token-lifecycle]

Once authenticated, the SDK manages token expiration and refresh automatically. When an access token is about to expire, the SDK uses the refresh token to obtain a new one before your next API call. You can subscribe to token refresh events with \`client.auth.onTokenRefresh()\` if you need to persist new tokens to your database.

Scopes [#scopes]

Every credential is associated with a set of scopes that define what it can access. Understanding scopes helps you follow the principle of least privilege.

Available Scopes [#available-scopes]

Scopes follow a \`resource:action\` pattern — \`resources:read\` lets you list and get resources, \`resources:write\` lets you create, update, and delete them. Other scopes include \`webhooks:manage\`, \`users:read\`, and \`analytics:read\`. Always request the minimum set your application actually needs.

Handling Permission Errors [#handling-permission-errors]

If your application attempts an operation outside its granted scopes, the SDK throws an \`AuthorizationError\` with a message explaining which scope is missing. This makes debugging straightforward — you'll know exactly what to add to your API key or OAuth request to resolve the issue.
`,a={contents:[{heading:void 0,content:`Authentication is the foundation of every interaction with the Voltus platform. Before your application can read or write any data, it needs to prove its identity. The SDK makes this seamless — you pass your credentials once during client initialization, and every subsequent request is automatically authenticated.`},{heading:`introduction`,content:`The Voltus API supports two authentication methods — API keys for server-side applications and OAuth 2.0 for user-facing integrations. Both methods are fully supported by the SDK, and you can switch between them without changing any of your data-fetching code.`},{heading:`choosing-a-method`,content:`If your application runs on a server and acts with its own identity, use an API key. If your application acts on behalf of individual users who need to grant permission, use OAuth 2.0. Most integrations start with API keys because they're simpler to set up, and migrate to OAuth later if user-level authentication becomes necessary.`},{heading:`security-principles`,content:`Regardless of which method you use, never expose credentials in client-side code, version control, or logs. Treat your API key like a password — store it in environment variables, rotate it periodically, and revoke it immediately if it's ever compromised.`},{heading:`api-keys`,content:`API keys are the simplest and most common way to authenticate with the Voltus API. You generate a key in the dashboard, pass it to the SDK, and you're ready to make requests.`},{heading:`key-types`,content:"Voltus issues three types of API keys. Secret keys start with `vlt_sk_` and grant full read-write access — use these only on the server. Public keys start with `vlt_pk_` and are restricted to read-only operations — these are safe for client-side code. Test keys start with `vlt_test_` and only work against the sandbox environment — use these during development."},{heading:`using-keys-in-the-sdk`,content:"Pass your API key to the `VoltusClient` constructor and the SDK handles everything else. It attaches the key as a Bearer token in the Authorization header of every request, validates the key format at initialization time, and throws a clear error if the key is malformed or missing."},{heading:`oauth-20`,content:`For applications that act on behalf of users, the SDK supports the full OAuth 2.0 authorization code flow with automatic token management.`},{heading:`the-authorization-flow`,content:"First, generate an authorization URL using `client.auth.getAuthorizationUrl()` with the scopes your application needs. Redirect the user to this URL — they'll see a consent screen showing what permissions your app is requesting. After they approve, Voltus redirects back to your callback URL with an authorization code that you exchange for access tokens."},{heading:`token-lifecycle`,content:"Once authenticated, the SDK manages token expiration and refresh automatically. When an access token is about to expire, the SDK uses the refresh token to obtain a new one before your next API call. You can subscribe to token refresh events with `client.auth.onTokenRefresh()` if you need to persist new tokens to your database."},{heading:`scopes`,content:`Every credential is associated with a set of scopes that define what it can access. Understanding scopes helps you follow the principle of least privilege.`},{heading:`available-scopes`,content:"Scopes follow a `resource:action` pattern — `resources:read` lets you list and get resources, `resources:write` lets you create, update, and delete them. Other scopes include `webhooks:manage`, `users:read`, and `analytics:read`. Always request the minimum set your application actually needs."},{heading:`handling-permission-errors`,content:"If your application attempts an operation outside its granted scopes, the SDK throws an `AuthorizationError` with a message explaining which scope is missing. This makes debugging straightforward — you'll know exactly what to add to your API key or OAuth request to resolve the issue."}],headings:[{id:`introduction`,content:`Introduction`},{id:`choosing-a-method`,content:`Choosing a Method`},{id:`security-principles`,content:`Security Principles`},{id:`api-keys`,content:`API Keys`},{id:`key-types`,content:`Key Types`},{id:`using-keys-in-the-sdk`,content:`Using Keys in the SDK`},{id:`oauth-20`,content:`OAuth 2.0`},{id:`the-authorization-flow`,content:`The Authorization Flow`},{id:`token-lifecycle`,content:`Token Lifecycle`},{id:`scopes`,content:`Scopes`},{id:`available-scopes`,content:`Available Scopes`},{id:`handling-permission-errors`,content:`Handling Permission Errors`}]},o=[{depth:2,url:`#introduction`,title:(0,n.jsx)(n.Fragment,{children:`Introduction`})},{depth:3,url:`#choosing-a-method`,title:(0,n.jsx)(n.Fragment,{children:`Choosing a Method`})},{depth:3,url:`#security-principles`,title:(0,n.jsx)(n.Fragment,{children:`Security Principles`})},{depth:2,url:`#api-keys`,title:(0,n.jsx)(n.Fragment,{children:`API Keys`})},{depth:3,url:`#key-types`,title:(0,n.jsx)(n.Fragment,{children:`Key Types`})},{depth:3,url:`#using-keys-in-the-sdk`,title:(0,n.jsx)(n.Fragment,{children:`Using Keys in the SDK`})},{depth:2,url:`#oauth-20`,title:(0,n.jsx)(n.Fragment,{children:`OAuth 2.0`})},{depth:3,url:`#the-authorization-flow`,title:(0,n.jsx)(n.Fragment,{children:`The Authorization Flow`})},{depth:3,url:`#token-lifecycle`,title:(0,n.jsx)(n.Fragment,{children:`Token Lifecycle`})},{depth:2,url:`#scopes`,title:(0,n.jsx)(n.Fragment,{children:`Scopes`})},{depth:3,url:`#available-scopes`,title:(0,n.jsx)(n.Fragment,{children:`Available Scopes`})},{depth:3,url:`#handling-permission-errors`,title:(0,n.jsx)(n.Fragment,{children:`Handling Permission Errors`})}];function s(e){let t={code:`code`,h2:`h2`,h3:`h3`,p:`p`,...e.components};return(0,n.jsxs)(n.Fragment,{children:[(0,n.jsx)(t.p,{children:`Authentication is the foundation of every interaction with the Voltus platform. Before your application can read or write any data, it needs to prove its identity. The SDK makes this seamless — you pass your credentials once during client initialization, and every subsequent request is automatically authenticated.`}),`
`,(0,n.jsx)(t.h2,{id:`introduction`,children:`Introduction`}),`
`,(0,n.jsx)(t.p,{children:`The Voltus API supports two authentication methods — API keys for server-side applications and OAuth 2.0 for user-facing integrations. Both methods are fully supported by the SDK, and you can switch between them without changing any of your data-fetching code.`}),`
`,(0,n.jsx)(t.h3,{id:`choosing-a-method`,children:`Choosing a Method`}),`
`,(0,n.jsx)(t.p,{children:`If your application runs on a server and acts with its own identity, use an API key. If your application acts on behalf of individual users who need to grant permission, use OAuth 2.0. Most integrations start with API keys because they're simpler to set up, and migrate to OAuth later if user-level authentication becomes necessary.`}),`
`,(0,n.jsx)(t.h3,{id:`security-principles`,children:`Security Principles`}),`
`,(0,n.jsx)(t.p,{children:`Regardless of which method you use, never expose credentials in client-side code, version control, or logs. Treat your API key like a password — store it in environment variables, rotate it periodically, and revoke it immediately if it's ever compromised.`}),`
`,(0,n.jsx)(t.h2,{id:`api-keys`,children:`API Keys`}),`
`,(0,n.jsx)(t.p,{children:`API keys are the simplest and most common way to authenticate with the Voltus API. You generate a key in the dashboard, pass it to the SDK, and you're ready to make requests.`}),`
`,(0,n.jsx)(t.h3,{id:`key-types`,children:`Key Types`}),`
`,(0,n.jsxs)(t.p,{children:[`Voltus issues three types of API keys. Secret keys start with `,(0,n.jsx)(t.code,{children:`vlt_sk_`}),` and grant full read-write access — use these only on the server. Public keys start with `,(0,n.jsx)(t.code,{children:`vlt_pk_`}),` and are restricted to read-only operations — these are safe for client-side code. Test keys start with `,(0,n.jsx)(t.code,{children:`vlt_test_`}),` and only work against the sandbox environment — use these during development.`]}),`
`,(0,n.jsx)(t.h3,{id:`using-keys-in-the-sdk`,children:`Using Keys in the SDK`}),`
`,(0,n.jsxs)(t.p,{children:[`Pass your API key to the `,(0,n.jsx)(t.code,{children:`VoltusClient`}),` constructor and the SDK handles everything else. It attaches the key as a Bearer token in the Authorization header of every request, validates the key format at initialization time, and throws a clear error if the key is malformed or missing.`]}),`
`,(0,n.jsx)(t.h2,{id:`oauth-20`,children:`OAuth 2.0`}),`
`,(0,n.jsx)(t.p,{children:`For applications that act on behalf of users, the SDK supports the full OAuth 2.0 authorization code flow with automatic token management.`}),`
`,(0,n.jsx)(t.h3,{id:`the-authorization-flow`,children:`The Authorization Flow`}),`
`,(0,n.jsxs)(t.p,{children:[`First, generate an authorization URL using `,(0,n.jsx)(t.code,{children:`client.auth.getAuthorizationUrl()`}),` with the scopes your application needs. Redirect the user to this URL — they'll see a consent screen showing what permissions your app is requesting. After they approve, Voltus redirects back to your callback URL with an authorization code that you exchange for access tokens.`]}),`
`,(0,n.jsx)(t.h3,{id:`token-lifecycle`,children:`Token Lifecycle`}),`
`,(0,n.jsxs)(t.p,{children:[`Once authenticated, the SDK manages token expiration and refresh automatically. When an access token is about to expire, the SDK uses the refresh token to obtain a new one before your next API call. You can subscribe to token refresh events with `,(0,n.jsx)(t.code,{children:`client.auth.onTokenRefresh()`}),` if you need to persist new tokens to your database.`]}),`
`,(0,n.jsx)(t.h2,{id:`scopes`,children:`Scopes`}),`
`,(0,n.jsx)(t.p,{children:`Every credential is associated with a set of scopes that define what it can access. Understanding scopes helps you follow the principle of least privilege.`}),`
`,(0,n.jsx)(t.h3,{id:`available-scopes`,children:`Available Scopes`}),`
`,(0,n.jsxs)(t.p,{children:[`Scopes follow a `,(0,n.jsx)(t.code,{children:`resource:action`}),` pattern — `,(0,n.jsx)(t.code,{children:`resources:read`}),` lets you list and get resources, `,(0,n.jsx)(t.code,{children:`resources:write`}),` lets you create, update, and delete them. Other scopes include `,(0,n.jsx)(t.code,{children:`webhooks:manage`}),`, `,(0,n.jsx)(t.code,{children:`users:read`}),`, and `,(0,n.jsx)(t.code,{children:`analytics:read`}),`. Always request the minimum set your application actually needs.`]}),`
`,(0,n.jsx)(t.h3,{id:`handling-permission-errors`,children:`Handling Permission Errors`}),`
`,(0,n.jsxs)(t.p,{children:[`If your application attempts an operation outside its granted scopes, the SDK throws an `,(0,n.jsx)(t.code,{children:`AuthorizationError`}),` with a message explaining which scope is missing. This makes debugging straightforward — you'll know exactly what to add to your API key or OAuth request to resolve the issue.`]})]})}function c(e={}){let{wrapper:t}=e.components||{};return t?(0,n.jsx)(t,{...e,children:(0,n.jsx)(s,{...e})}):s(e)}export{i as _markdown,c as default,r as frontmatter,a as structuredData,o as toc};